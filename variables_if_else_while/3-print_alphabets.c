#include <stdio.h>
/**
* main- Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
        char alphabet = 'a';
	char Alphabet = 'A';

        while (alphabet <= 'z')
        {
                putchar("%c", alphabet);
                alphabet++;
	}
	while (Alphabet <= 'Z'
		putchar("%c", alphabet);
		Alphabet++
        }
        {
                putchar("\n");
        }
        return (0);
}                                                           
